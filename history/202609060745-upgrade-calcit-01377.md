# Upgrade to Calcit 0.13.77

- Migrated the legacy editor/compact snapshots to the canonical Snapshot format.
- Added explicit Calcit and `@calcit/procs` 0.13.77 toolchain metadata.
- Updated `read-file` to the current single-argument API.
- Added pinned CI validation for dependencies, formatting, preprocessing, static quality, deprecated APIs, dynamic dispatch, and a word-filter behavior smoke test.

The former `compact.cirru` remains recoverable from Git history; `calcit.cirru` is now the single source of truth.
