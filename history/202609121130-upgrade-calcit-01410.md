# Upgrade to Calcit 0.14.10

- Pin Calcit and `@calcit/procs` to 0.14.10.
- Add typed `Ref`/`Fn` schemas instead of whole-Dynamic.
- Replace legacy `.trim` / `.split-lines` / `.join-str` method calls with named functions.
- `--check-only`, quality baseline, dynamic-methods and the word-filter smoke test pass.
